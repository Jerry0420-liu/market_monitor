# Persistence package

M1 provides SQLite durability, migrations, single-writer transactions, content-addressed
artifacts, diagnostics, and online backup foundations. It contains no market-domain tables.

Run the targeted suite with:

```text
python scripts/dev.py test-m1
```
