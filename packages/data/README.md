# Data package

Provider-neutral reference and acquisition services. The checked-in fixture/replay provider is
enabled for deterministic tests; the live-provider shell is disabled and contains no credentials.
