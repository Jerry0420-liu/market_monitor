# Runtime data

This directory is intentionally empty in the source tree except for this README. The application
creates runtime databases, logs, caches, artifacts, and backups here when it runs. They are local
operational data, not source files, and must not be committed.
