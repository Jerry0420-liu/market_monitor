# Documentation

This is the documentation entry point. [`acceptance/CURRENT_STATUS.md`](acceptance/CURRENT_STATUS.md)
is the only current status source; historical reports never override it.

## Architecture

Current architecture and contracts are in [`architecture/`](architecture/).

## Decisions

Approved Change Requests and owner decisions are in [`decisions/`](decisions/).

## Development

Contribution workflow and local verification are in [`development/`](development/).

## Operations

Deployment, backup, restore, recovery, and data-use notices are in [`operations/`](operations/).
Production instrument-reference bootstrap instructions are in
[`operations/REFERENCE_BOOTSTRAP.md`](operations/REFERENCE_BOOTSTRAP.md).

## Acceptance

Current acceptance material is in [`acceptance/`](acceptance/). The current status file is the
only status source.

## Archive

Superseded reports and replaced release material are in [`archive/`](archive/) for provenance only.
